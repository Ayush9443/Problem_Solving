class Solution {
public:
    int alternatingSum(vector<int>& nums) {
        int num = 0;
        for (int i = 0; i < nums.size(); i++) {
            if (i % 2 == 0)
                num += nums[i];
            else
                num -= nums[i];
        }
        return num;
    }
};
